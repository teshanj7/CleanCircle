<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" /> <!-- <meta-data -->
    <!-- android:name="com.google.android.gms.version" -->
    <!-- android:value="@integer/google_play_services_version" /> -->

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.UEEProjCleanCircle"
        tools:targetApi="31">

        <!--
             TODO: Before you run your application, you need a Google Maps API key.

             To get one, follow the directions here:

                https://developers.google.com/maps/documentation/android-sdk/get-api-key

             Once you have your API key (it starts with "AIza"), define a new property in your
             project's local.properties file (e.g. MAPS_API_KEY=Aiza...), and replace the
             "YOUR_API_KEY" string in this file with "${MAPS_API_KEY}".
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="${MAPS_API_KEY}" />

<!--        <activity-->
<!--            android:name=".activities.WorkerHomeActivity"-->
<!--            android:exported="false" />-->
        <activity
            android:name=".activities.CitizenHomeActivity"
            android:exported="false" />
<!--        <activity-->
<!--            android:name=".activities.CurrentLocationActivity"-->
<!--            android:exported="false">-->

<!--        </activity>-->
        <activity
            android:name=".activities.PaymentPageActivity"
            android:exported="false" /> <!-- <activity -->
        <!-- android:name=".activities.SignUpActivity" -->
        <!-- android:exported="false" > -->
        <!-- </activity> ok -->
        <activity
            android:name=".activities.IndexPageActivity"
            android:exported="false" />
        <activity
            android:name=".activities.SplashScreenActivity"
            android:exported="false" />
        <activity
            android:name=".activities.LoginPageActivity"
            android:exported="false" />
<!--        <activity-->
<!--            android:name=".activities.AdminHomeActivity"-->
<!--            android:exported="false" />-->
<!--        <activity-->
<!--            android:name=".activities.WasteWorkerHomeActivity"-->
<!--            android:exported="false" />-->
        <activity
            android:name=".activities.SignUpActivity"
            android:exported="false" />
        <activity
            android:name=".activities.ReportPageActivity"
            android:exported="false" />
        <activity
            android:name=".activities.ReportInsertActivity"
            android:exported="false" />
        <!--        <activity-->
        <!--            android:name=".activities.ReportFetchingActivity"-->
        <!--            android:exported="false" />-->
        <activity
            android:name=".activities.ReportList"
            android:exported="false" />
    </application>

</manifest>